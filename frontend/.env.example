# Прод: только origin бэкенда, без пути; /api/v1 подставляется в client.ts.
# Пример: https://parent-med-production-backend.up.railway.app
# Задавайте в Railway Variables до сборки фронта.
# Локально не задавайте — /api/v1 + прокси Vite → localhost:8000
# VITE_API_URL=
