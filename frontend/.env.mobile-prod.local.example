# Локальные prod-переменные для iOS/TestFlight сборки.
# Скопируйте в `.env.mobile-prod.local` и заполните значениями.
# Файл `.env.mobile-prod.local` в git не попадает (игнорируется).

VITE_API_URL=https://parent-med-production.up.railway.app
VITE_MARKETING_SITE_URL=https://parent-med-production-frontend.up.railway.app/
VITE_HITKEEP_SCRIPT_URL=https://your-hitkeep-domain/hk.js
